# UV automatically manages virtual environments
# This file ensures the project directory is recognized by direnv

# Set Python path to use UV's managed environment
export PYTHONPATH="${PWD}:${PYTHONPATH}"

# Ensure UV uses the project's Python version
export UV_PYTHON_VERSION="3.13"
